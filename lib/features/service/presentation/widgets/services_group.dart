import 'package:flutter/material.dart';

class ServicesGroup extends StatelessWidget {
  const ServicesGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.remove_red_eye);
  }
}